<!DOCTYPE html>
<html>
  <head>
    <title>ジャンル追加・一覧画面</title>
  </head>
<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>ジャンル追加・一覧画面</h1>
      <%= form_for(@genre, url: admin_genres_new_path) do |f| %>
        <div class="name">ジャンル
        <%= f.text_field :name %>
        <input type="radio" name="example" value="有効">有効
        <input type="radio" name="example" value="無効">無効
        <%= f.submit "追加", class:"btn btn-change btn-xs" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
</html>