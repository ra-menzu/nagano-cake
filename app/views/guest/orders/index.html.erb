<!DOCTYPE html>
<html>
  <head>
    <title>注文履歴一覧</title>
  </head>
  <body>
    <div class="container">
    <h2>注文履歴一覧</h2>
    <div class="row">
    <table border="1" rules="all"class="table table-striped">
    <thead>
      <tr>
        <th>注文日</th>
        <th>配送先</th>
        <th>注文商品</th>
        <th>支払額</th>
        <th>ステータス</th>
        <th>注文詳細</th>
      </tr>
    </thead>
    <tbody>
    <% current_guest.orders.each do |o| %>
    <% o.products.each do |product| %>
        <tr>
            <td><%= o.created_at.strftime("%Y年%-m月%-d日") %></td>
            <td>〒<%= o.postal_code %><%= o.postal_adress %><%= o.destination %></td>
            <td><%= product.name %></td>
            <td><%= o.billing_amount %>円</td>
            <td><%= o.order_status %></td>
            <td><%= link_to "表示する" ,guest_order_path(o.id),class:"btn btn-info" %></td>

        </tr>
      <% end %>
      <% end %>
    </tbody>
    </table>
    </div>
</html>

