<h1>ケーキ(全X件)</h1>

		<% @products.each do |product| %>
			<div class="top-product-cake">
				<img class="sample_cake" src="top-image.jpg">
				<h4 class="top-product-name"><%= @product.name %></h4>
				<%= products.non_tax_price %>
			</div>
		<% end %>